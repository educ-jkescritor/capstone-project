import DashboardView from '../components/DashboardView';

export const metadata = {
  title: 'Dashboard — TELECASH',
};

// Server Component by default
export default function DashboardPage() {
  return <DashboardView />;
}
