import AnalyticsView from '../../components/AnalyticsView';

export const metadata = {
  title: 'Analytics & Reports — TELECASH',
};

// Server Component by default
export default function AnalyticsPage() {
  return <AnalyticsView />;
}
