import LedgerView from '../../components/LedgerView';

export const metadata = {
  title: 'Ledger — TELECASH',
};

// Server Component by default
export default function LedgerPage() {
  return <LedgerView />;
}
