import SettingsView from '../../components/SettingsView';

export const metadata = {
  title: 'Settings — TELECASH',
};

// Server Component by default
export default function SettingsPage() {
  return <SettingsView />;
}
